org.gradle.jvmargs=-Xmx4096m -XX:MaxMetaspaceSize=1024m -Dfile.encoding=UTF-8
org.gradle.configuration-cache=true
org.gradle.caching=true
android.useAndroidX=true
kotlin.code.style=official

# Release identity — resolved by build.gradle.kts via findProperty().
# CI / Justfile pass -PversionName / -PversionCode to override these for
# local development builds. For source-only builds (F-Droid), these values
# are the source of truth and must be kept in sync with git tags.
versionName=1.2
versionCode=10200
